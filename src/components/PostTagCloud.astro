---
import '../styles/tagcloud.css';
const { tags } = Astro.props;
---
  <div class="tags">
    {tags.map((tag: string) => (
      <p class="tag"><a href={`/tags/${tag}`}>{tag}</a></p>
    ))}
  </div>